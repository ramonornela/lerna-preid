{
	"name": "preid-version",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"canary": "lerna publish --canary=alpha --skip-git --yes",
		"prerelease": "lerna publish --skip-git --yes --cd-version=prerelease"
	},
	"devDependencies": {
		"lerna": "^2.2.0"
	}
}
